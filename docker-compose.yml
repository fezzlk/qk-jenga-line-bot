services:
  app:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env.local
    command: ["npm", "run", "dev"]
    restart: unless-stopped
