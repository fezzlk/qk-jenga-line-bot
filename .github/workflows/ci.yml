name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # TODO: スタックに合わせてセットアップ・lint・testステップを追加
      # 例: Node.js
      # - uses: actions/setup-node@v4
      #   with:
      #     node-version: '20'
      # - run: npm ci
      # - run: npm run lint
      # - run: npm test

      # 例: Python
      # - uses: actions/setup-python@v5
      #   with:
      #     python-version: '3.12'
      # - run: pip install -r requirements.txt
      # - run: python -m pytest

      - run: echo "TODO: lint・testコマンドを設定してください"
