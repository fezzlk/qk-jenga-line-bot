# ゲーム仕様（独自コンセプト）

> 名称は暫定で「グラグラタワー」を使用。正式名称は [`docs/naming-candidates.md`](./docs/naming-candidates.md) を参照し、[FEZ-33](https://linear.app/fezzlk/issue/FEZ-33/名称企画を再検討するjengaqk謎解きジェンガの言及を排除)でユーザー承認後に確定する。

## 目的

物理的な積み木・専用機材を一切使わず、LINEグループチャットだけで「積み木を抜いていくと崩れる」というリスク管理型の緊張感を再現するミニゲーム。QuizKnock「謎解きジェンガ」からは「積み上げ式ブロックゲーム」というコンセプトのみを参考にし、実装・ルール文言・演出は独自に設計する。

## 想定利用シーン

LINEグループでの飲み会・オフ会・待ち時間などに、参加者が交代でBotへ「引く」操作を送り合って遊ぶ。物理的な積み木を持ち寄る必要がないため、オンライン参加者が混在するグループでも成立する。

## ゲームの流れ（案）

1. グループ内の誰かがBotへ「タワー開始」を送るとゲームが始まる。
2. Botは内部で「安全本数」（例: 初期値20）を保持する。
3. 参加者は順番に「引く」を送る。1回引くごとに安全本数が1減り、かつ残り本数が少ないほど「崩落」する確率が上がる（例: 残り10本以降、抜くたびに崩落抽選を行う）。
4. 崩落が発生した時点で引いた人が「タワーを崩した人」となり、そのラウンドの結果が確定する。
5. 結果をグループに通知し、崩した回数・生き残った回数などの簡易スコアを記録する（永続化方式は未確定、下記「未決定事項」参照）。

## 想定コマンド（LINE Bot）

| コマンド例 | 内容 |
|---|---|
| `タワー開始` | 新しいラウンドを開始する |
| `引く` | 自分の番として1本引く（崩落判定を行う） |
| `タワー状況` | 現在の安全本数・進行状況を確認する |
| `タワー結果` | 直近ラウンドの結果、通算成績を表示する |

コマンド文言・呼称は仮であり、確定名称に合わせて調整する。

## データモデル（案）

- `TowerRound`: グループID、開始日時、現在の安全本数、参加者ごとの「引いた回数」、崩落した人、終了日時
- `PlayerStats`: グループID、ユーザーID、通算プレイ回数、通算「崩した」回数

具体的な永続化先（Firestore等）は「未決定事項」を参照。

## 非対象・非スコープ

- 実際の物理積み木やAR演出は扱わない（完全にテキストベースのやり取りで完結させる）。
- QuizKnock固有のクイズ問題文・演出台本は一切流用しない。

## 未決定事項（このissueのスコープ外、別途判断が必要）

- 正式名称の確定（[`docs/naming-candidates.md`](./docs/naming-candidates.md)）
- データ永続化先（`.scaffold-config`はFirestore/Firebase Authを想定済みだが未実装。グループ単位の状態管理で足りるため、必ずしもユーザー認証が必要とは限らず、要件と併せて再検討する）
- 崩落確率のバランス調整（初期値・増加カーブは仮の数値であり、実装時にチューニングが必要）
- クイズ・チャレンジ要素を追加するか（現状案は「引く」だけのシンプルなリスクゲームだが、QuizKnock色を排除した独自のミニチャレンジを足すかどうかはユーザー判断）
- 課金方式・公開時期・想定顧客（このissueの完了条件である「公開可否の判断」に該当し、名称・仕様確定後にあらためて判断する）
