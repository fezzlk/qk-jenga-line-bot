# ゲーム仕様（独自コンセプト）

> 名称は暫定で「グラグラタワー」を使用。正式名称は [`docs/naming-candidates.md`](./docs/naming-candidates.md) を参照し、[FEZ-33](https://linear.app/fezzlk/issue/FEZ-33/名称企画を再検討するjengaqk謎解きジェンガの言及を排除)でユーザー承認後に確定する。
>
> **要注意**: 下記の核心メカニクス（隠された問題を少しずつ開示し、正解に至ったタイミングで結果が決まる）は、名称変更だけでは解消しない、QuizKnock「謎解きジェンガ」の実際のゲーム性そのものへの類似という論点を含む。2026-08-29時点でユーザーに共有済みだが、対応方針は未確定（FEZ-33参照）。

## 目的

LINEグループチャットだけで、「隠された1つの問題（画像 or 文章）を少しずつ開示していき、解答者がどのタイミングで正解にたどり着くか」という緊張感を再現するミニゲーム。物理的な積み木・専用機材は使わない。「ジェンガ」「ブロック」「タワー」という言葉は、開示を1ステップ進める操作の比喩として使っているだけで、実装上ブロックを物理的に引く/積む処理があるわけではない。

## 想定利用シーン

LINEグループでの飲み会・オフ会・待ち時間などに、複数人が協力して問題を仕込み（作成フェーズ）、別の参加者がそれに挑戦する（解答フェーズ）形で遊ぶ。

## 全体の流れ

1. **作成フェーズ**: 解答者不在（別グループ、または一時退出させたグループ）で、複数の問題作成者が1つの完成した問題（画像 or 文章）を少しずつ隠していく。隠す場所がなくなったら作成完了。
2. **開始/招待**: 解答者を招待する、または解答者のいる別グループに切り替えて挑戦を開始する。
3. **解答フェーズ**: 隠された問題が1ステップずつ、参加している解答者全員に開示される。各ステップに対して解答者は誰でも回答を試みることができ（先着順）、最初に正解した時点でそのステップ・セッション全体の結果が確定する。以下のいずれかの条件で挑戦セッション全体が終了する（終了条件はすべて同列に「解答者（チーム）アウト」として扱う。詳細は「ユースケース: 解答」を参照）。

## ユースケース: 問題作成

- **アクター**: 問題作成者（複数人、事前にA→B→Cのような順番を決めておく）
- **事前条件**: 解答者はグループにいない（別グループ、または一時退出済み）。土台となる問題（画像 or 文章）が用意されている。v1では問題作成者がBotとのやり取りで都度アップロード（画像）または入力（文章）する方式のみをサポートする。あらかじめ用意されたテンプレートから選ぶ方式は将来的な拡張として検討する（v1スコープ外）。
- **トリガー**: 問題作成者の1人が、Botとのやり取りで「隠す場所」を1つ選ぶ操作を送る

**基本フロー**
1. 決められた順番（例: A→B→C→A→B→C→…のラウンドロビン）で、問題作成者が1人ずつBotとやり取りする。
2. 各自の手番で、まだ隠されていない場所を1つ選んで隠す（画像なら特定マスを隠す、文章なら特定の文字を削除する）。この1回の操作を「ブロック1本」に見立てる。
3. 隠す総数（問題の構造で決まる、例: 25マス/25文字）分をすべて隠し終えたら作成完了。

**事後条件**: 隠す順序（開示時は逆順に使われる想定）が確定した状態のパズルラウンドが1件、作成完了の状態で存在する。

## ユースケース: 解答

- **アクター**: 解答者（複数人）。v1から複数人が同時に参加できる。1つの開示ステップに対しては、参加している解答者の誰でも回答を試みることができ、最初に正解した人の回答時点でそのステップ・そのセッション全体の結果（後述の終了条件を参照）が確定する（先着順）。回答したのが誰であっても、結果は参加者全員に共通で適用される。
- **事前条件**: 作成フェーズが完了しているパズルラウンドが存在する。解答者が招待済み、または解答者のいるグループで開始されている。
- **設定値**（ラウンドごとにカスタム値を指定可能。以下は既定値）:
  - **誤答上限数**（既定値: 3）: 参加している解答者全員の誤答を合算した「チーム共有カウント」として扱う（個人ごとのカウントではない。先着順で複数人が挑む前提のため）。累計がこの数に達すると、その時点で終了する。
  - **1ブロックでの可能回答数**（既定値: 1＝1答のみ）: 1つの開示ステップに対して、参加解答者全体で許容される回答試行回数の上限（個人ごとではなく、ブロック単位で共有する合計）。上限に達すると次のブロックの開示に強制的に進む。

**基本フロー**
1. Botが問題を1ステップぶん、参加している解答者全員に開示する（作成時に隠した順序の逆順で、1箇所ずつ明らかにする）。
2. 開示中、参加している解答者は誰でも回答を試みるか、試みずスキップに相当する形で待つかを選べる。
3. 誰かが回答した場合:
   - 正解であれば、その開示ステップの時点で挑戦セッション全体の結果が確定する（後述の終了条件を参照）。誰が正解したかに関わらず結果は参加者全員に共通で適用される。
   - 誤答であれば、チーム共有の誤答カウントを1増やす。「1ブロックでの可能回答数」の上限に達していなければ、同じブロックに対して（誰でも）再度回答できる。上限に達していれば次のブロックの開示に進む。
4. 誰も回答を試みない場合でも自動的には次に進まない（タイムアウトは設けない）。参加解答者の誰かが明示的に「スキップ」操作を送った場合のみ、そのブロックでの回答は行わず次のブロックの開示に進む。
5. 隠した総数ぶんすべて開示し終えるまで、1〜4を繰り返す。

**終了条件（以下はすべて「解答者（チーム）アウト」として同列に扱う）**
- (a) 正解した時点。誰が正解したかに関わらず、その開示ステップが結果的に「アウトのブロック」として記録される。どのブロックが「アウト」になるかは作成者・解答者の誰にも事前に決められず、参加解答者の実力・閃きのタイミングのみに依存する。
- (b) 累計誤答数（チーム共有）が「誤答上限数」に到達した時点。
- (c) 隠した総数ぶんすべて開示し終えても、正解にも誤答上限到達にも至らなかった場合。

誤答上限を設けている理由: 「正解した時点でアウト」というルールだけだと、解答者があえて誤答し続けて正解を避け、挑戦の終了自体を引き延ばす戦略が成立してしまう。それを防ぐため、誤答にもペナルティ（誤答上限到達でアウト）を課している。

**事後条件**: パズルラウンドが終了状態になり、そのセッションに参加していた解答者全員に暫定スコアが記録される。v1の既定配点は終了条件別に、(a) 正解した時点で終了: +1点、(b) 誤答上限到達で終了: -1点、(c) 全開示未決着で終了: 0点、とする。ラウンドごとの設定でこれとは異なる配点に変更できる。

## 想定コマンド（LINE Bot、暫定・要検討）

物理的にブロックを「引く」動作は存在しないため、旧版の「引く」中心のコマンド体系は撤回し、以下のように暫定変更する。文言は名称確定（FEZ-33）に合わせて調整する。

| コマンド例 | フェーズ | 内容 |
|---|---|---|
| `問題作成開始` | 作成 | パズルラウンドを新規作成する（解答者不在のグループで実行） |
| `隠す [位置]` | 作成 | 手番の問題作成者が、指定した1箇所を隠す |
| `作成状況` | 作成 | 残り隠せる場所・現在の手番を確認する |
| `挑戦開始` | 解答 | 作成済みのパズルラウンドへの挑戦を開始する（解答者のいるグループで実行） |
| `回答 [内容]` | 解答 | 現在開示されているステップに対して回答する（参加解答者の誰でも送信可） |
| `スキップ` | 解答 | 回答せず次のステップの開示に進む |
| `挑戦状況` | 解答 | 現在の開示状況・誤答数を確認する |
| `結果` | 解答 | 直近ラウンドの結果、通算スコアを表示する |

## データモデル（案）

- `PuzzleRound`: グループID、元の問題（画像 or 文章の参照）、隠す総数、問題作成者の順番リスト、隠された位置のリスト（隠した順）、作成完了フラグ、開始日時、誤答上限数（ラウンド設定値）、1ブロックでの可能回答数（ラウンド設定値）、点数配分設定（既定値は正解=+1点/誤答上限到達=-1点/全開示未決着=0点、ラウンドごとに上書き可能）
- `AnswerSession`: `PuzzleRound`への参照、参加解答者IDのリスト（複数人対応）、現在の開示ステップ位置、累計誤答数（チーム共有）、現在ステップでの回答試行回数（ブロック単位で共有）、終了理由（正解／誤答上限到達／全開示）、終了日時
- `PlayerStats`: グループID、ユーザーID（Firebase Authのuidに対応）、通算プレイ回数、通算スコア（終了条件別の点数配分に基づく合計）

**永続化先**: Firestore + Firebase Authを採用する。ユーザー識別にはFirebase Authを使い、LINEユーザーIDとFirebase Authのユーザーを紐づける（具体的な紐付け方式は実装時に設計）。

## 非対象・非スコープ

- 実際の物理積み木やAR演出は扱わない（完全にテキストベースのやり取りで完結させる）。
- QuizKnock固有のクイズ問題文・演出台本は一切流用しない。

## 未決定事項（このissueのスコープ外、別途判断が必要）

- 正式名称の確定（[`docs/naming-candidates.md`](./docs/naming-candidates.md)）
- 「隠された問題を少しずつ開示し正解タイミングで結果が決まる」という核心メカニクスが、QuizKnock「謎解きジェンガ」の実際のゲーム性への類似という論点を残したままで良いか（名称変更のみで足りるか、企画自体の見直しが必要か）
- 誤答カウント・回答試行回数を「チーム共有」とする設計は今回の想定であり、実際に複数人プレイを試してみて個人ごとのカウントの方が体験として良いと分かった場合は見直す
- 課金方式・公開時期・想定顧客（このissueの完了条件である「公開可否の判断」に該当し、名称・仕様確定後にあらためて判断する）
